using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TintImageScript : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        transform.SetAsLastSibling();
    }
}
